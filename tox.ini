[tox]
envlist = py37

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
    nose
    coverage
commands =
    # NOTE: you can run any command line tool here - not just tests
    coverage erase
    nosetests eero/tests/* --with-coverage
    coverage run  {envbindir}/report
    coverage html
